version: 3

build:
  os: ubuntu-20.04
  tools:
    python: "3.11"

python:
  install:
    - method: pip
      path: .
      extra_args:
        - "--no-deps"
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/conf.py
